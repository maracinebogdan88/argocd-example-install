apiVersion: v1
kind: Service
metadata:
  name: rollouts-demo-svc
spec:
  type: NodePort
  ports:
    - port: 30000  # The port exposed on nodes
      targetPort: port-3000 # The port your application is running on
      protocol: TCP
      name: port-3000
  selector:
    app: rollouts-demo